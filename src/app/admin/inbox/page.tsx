import { AdminInbox } from "@/components/admin/AdminInbox";

export const metadata = {
  title: "Inbox — Robert Editing",
  robots: { index: false, follow: false },
};

export default function AdminInboxPage() {
  return <AdminInbox />;
}
